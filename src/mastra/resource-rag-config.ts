import { RESOURCE_EMBEDDING_DIMENSION, models } from "@/mastra/models";

export { RESOURCE_EMBEDDING_DIMENSION };
export const RESOURCE_EMBEDDINGS_INDEX = "resource_embeddings";
export const resourceEmbeddingModel = models.embedding;
