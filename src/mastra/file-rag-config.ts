import { FILE_EMBEDDING_DIMENSION, models } from "@/mastra/models";

export { FILE_EMBEDDING_DIMENSION };
export const FILE_EMBEDDINGS_INDEX = "file_embeddings";
export const fileEmbeddingModel = models.embedding;
